<div class="container mx-auto p-4">
  <h1 class="text-2xl font-bold mb-4">読んだ本</h1>

  <% if @already_read_books.any? %>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <% @already_read_books.each do |book| %>
        <div class="border rounded-lg p-4">
          <p>ISBN: <%= book.isbn %></p>
          <%= button_to already_read_book_path(book.isbn), method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded hover:bg-red-700" do %>
            読んだ本を解除
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <p>読んだ本はまだありません。</p>
  <% end %>
</div>